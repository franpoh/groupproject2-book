const { Reviews } = require("../connect.js");

module.exports = {
    addReview: async () => {
        let result = {
            message: null,
            status: null,
            data: null,
        };




        return result;
    },
};